# Bundle patch for @just-genius/dsh-session-navigator.
#
# Installing this package joins it to the profile's bundle stack because
# package.json declares `dsh.bundle.patch`. This patch inserts the plugin's
# loader row into the tree; the row's `name` is the module specifier the
# loader imports (the package's own name). No host `config` is needed: the
# host body is empty and all behavior lives in the browser half.

- insert:
    - id: session-navigator
      name: '@just-genius/dsh-session-navigator'
