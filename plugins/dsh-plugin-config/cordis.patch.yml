# Bundle patch for @just-genius/dsh-plugin-config.
#
# Replaces the official read-only Plugin list tab with a single Plugins page:
# collapsible installed inventory + awesome-dsh-plugin marketplace.

- insert:
    - id: dsh-plugin-config
      name: '@just-genius/dsh-plugin-config'

- id: ui-settings-plugin-inventory
  disabled: true
