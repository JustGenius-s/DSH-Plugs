# Bundle patch for @just-genius/dsh-plugin-config.
#
# Adds the Plugin extensions tab alongside DSH's own plugin pages.
# Keep the official inventory and sidebar plugin manager enabled.

- insert:
    - id: dsh-plugin-config
      name: '@just-genius/dsh-plugin-config'
