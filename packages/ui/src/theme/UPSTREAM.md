# Theme snapshot provenance

These files are a vendored snapshot of
`deepseek-harness/packages/client/ui-theme/src/styles`.

Normal package builds consume this committed snapshot. To refresh it, point
`DSH_THEME_SRC` at an explicit DeepSeek Harness checkout and run the
`sync-theme` script, then review and commit the generated diff.
